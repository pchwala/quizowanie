1. use capacitor, only android for now.
2. consider if this is a good idea - questions, answers, categories in local db for faster loading and lowering DB resources usage. all the other tables in external DB
3. we want user to be able to use the app offline. consider what happens when user uses app offline. how to store and then sync session and study data with xternal DB
4. consider what to do when update to questions happens. how to update the already installed user apps
5. plan notifications system
